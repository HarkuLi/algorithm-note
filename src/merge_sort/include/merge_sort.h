#ifndef MERGE_SORT_INCLUDE_MERGE_SORT_H_
#define MERGE_SORT_INCLUDE_MERGE_SORT_H_

#include <vector>

void MergeSort(std::vector<int>*, std::size_t, std::size_t);
void Merge(std::vector<int>*, std::size_t, std::size_t, std::size_t);

#endif
